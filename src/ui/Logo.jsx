import { Link } from 'react-router-dom';

function Logo() {
  return (
    <Link to="/" className="tracking-wides text-xl font-semibold uppercase">
      PTruyen
    </Link>
  );
}

export default Logo;
